# CONTEXTO — Netrunner — Universal Agent SDK

> Funcionalidades excepcionales, gotchas, data importante para recordar
> cuando se trabaja en ESTE repo. (No estado — conocimiento.)

## Funcionalidades excepcionales
- SDK universal: un solo binario convierte CUALQUIER proyecto en agente-operable para CUALQUIER agente.
- Un solo motor es a la vez AI SDK + MCP server + skill + plugin + grafo + control agéntico (vistas del mismo contrato de tools).
- Auto-mejora interna (background review fork + curator determinista + Memento-Skills) anclada a señal externa.

## Gotchas / trampas conocidas
- Este repo es OPEN-SOURCE público: NUNCA commitear secrets, .env ni credenciales.
- El motor se construye en fases posteriores. Esta inicialización (repo + .onyx + spec.md + DEC-001) es el CONTRATO BASE — no inventar código fuera de las fases.
- El contrato de tools `src/core` es el núcleo: no romperlo sin versionar (extensibilidad).
- **CI**: GitHub Actions está DESACTIVADO (billing de la cuenta). El gate real es el CI local (`scripts/ci-local.sh` + hooks). Workflows versionados en `scripts/ci-github/` (restaurar cuando se resuelva billing).
- **CI local NO revienta la PC (OOM fix 2026-08-26):** `pre-push` corre SOLO el gate rápido (typecheck+lint+test, ~4s). Mutation+act se corren bajo demanda con `./scripts/ci-local.sh --full` (release/merge), NO en cada push. Stryker `concurrency: 2` + reporters solo clear-text. Vitest `maxWorkers: 4`.

## Data importante para recordar
- **SCOPE (directiva de Cris, 2026-08-26):** Netrunner es una herramienta de "uso COMPLETO para el agente" — dentro hay MUCHAS complejidades que deben manejarse de forma ÓPTIMA y GENERAL, para que funcione para CUALQUIER caso de uso. Este scope debe mantenerse en .onyx y en la implementación. Cada decisión de código debe preguntarse: ¿es óptimo y general, o solo resuelve un caso?
- **INVESTIGACIÓN 2026-08-26 (mina cyberpunk + repos/papers)**: guardado en `.doc/auditoria/20260826_mina-cyberpunk-30-conceptos.txt` y `20260826_repos-papers-gaps.txt`. La ficción ya se parece a Netrunner (grafo=matrix, disclosure=floors, curator=daemon). Features priorizadas de la mina (30 conceptos con URL validada): `map` (grafo visual), `depth` (disclosure por niveles), `persist` (decisiones que sobreviven sesión), `bd` (auto-docs que reproducen el repo), `scan` (overlay servicios/versiones/cobertura), `snapshot` (rollback), `guard` (Black ICE: protecciones). Repos/papers que validan y dan solución a los gaps: colbymchenry/codegraph, Graphify-Labs/graphify, isomorphic-git, simple-git, vitest-coverage-report-action (cobertura), leasot (TODOs), readme-ai (docs auto), mermaid (grafo visual), commitlint + papers arXiv (Less Context Better Agents 2606.10209, Codebase-Memory 2603.27277, LATM 2305.17126, Reflexion 2303.11366, Cannot-Self-Correct 2310.01798, CoALA 2309.02427).
- **Bugs P0 confirmados por el conejillo (corregir YA):** (A) colisión de IDs en grafo: `graph.ts` indexa `def:<name>` sin namespacing por archivo → defs reales de `src/` SOBREESCRITOS por copias `.stryker-tmp` (demo-hvac: 1667 nodes reportados, 510 filas en DB). Fix: namespacing por archivo + excluir `.stryker-tmp`/`node_modules`/`coverage` de IGNORED_DIRS. (B) MCP server muere al instante en stdio real (exit 0 sin responder initialize). (C) skill instalada en `.netrunner/` no visible para OpenCode (busca `.opencode/skills`, `.claude/skills`, global).
- **Wave 6 propuesta (skill auto-generante que documenta todo)**: detectores PURE `git.ts` (branch/remote/tags), `versions.ts` (versiones por categoría prod/dev), `coverage.ts` (% vitest json-summary), `services.ts` (urls/puertos), `dirs.ts` (árbol), `todos.ts` (TODO/FIXME) + snapshot store `state/project.json` + MCP resources (`net://meta/*` spec 2026-07-28) + generador de doc viva (README/AGENTS desde snapshot) conectado al curator.
- **2026-08-26: Wave 2 (núcleo universal) — plugin system real**: `src/core/plugin.ts` — extension-points (`registerTool`/`onContext`/`policyHook`) + efectos REVERSIBLES (patrón Cordis/AutoGen/LATM): `loadPlugin` registra tools y devuelve handle, `unloadPlugin` las deshace. `ToolRegistry.unregister()` agregado. Idempotente (no duplica). El núcleo NO importa plugins (sin acoplamiento inverso). Gherkin `features/plugin-system.feature`. 72 tests verdes (17 archivos).
- **2026-08-26: Wave 2 — ops (control determinista)**: `src/tools/ops.ts` — `runOp(kind, dir)` ejecuta test/build/lint del stack con señal externa real (exit code), respetando el packageManager detectado (pnpm/npm/bun/yarn por lockfile). Tools `op.test`/`op.build`/`op.lint` (family op, readOnly=false) registradas en el contrato. Es el "control autónomo" que Cris quiere (AC-6). Gherkin `features/ops.feature`. 75 tests verdes (18 archivos).
- **2026-08-26: Wave 2 — policy por intención + secrets scopeados**: `src/policy/index.ts` — `evaluatePolicy` con `Intent` tipado (explore/edit/destroy/ops), read→allow, mutate sin approval→deny (fail-closed); `resolveSecrets(projectDir, toolId)` lee `.netrunner/secrets.json` scopeado SOLO a la tool (nunca al LLM, Mandamiento 7). Gherkin `features/policy-intent.feature`. 78 tests verdes (19 archivos).
- **2026-08-26: Wave 2 — matriz stack→toolsets declarativa**: `src/transport/toolsets.ts` — `STACK_TOOLSETS` (matriz declarativa {stack, toolsets}) + `toolsetsForStack(stack)`. Reemplaza el hardcode de TOOLSETS en mcp-server: TS activa graph+stack+ops, python/go/rust activan graph+stack. Extensible sin tocar mcp-server. Gherkin `features/toolsets.feature`. **Wave 2 (núcleo universal) COMPLETA: plugin system + ops + policy + matriz.** 81 tests verdes (20 archivos).
- **2026-08-26: Wave 3 (skill auto-generante) — detector git**: `src/context/git.ts` — `gitInfo(dir)` lee `.git/HEAD` + `.git/config` + `.git/logs/HEAD` DIRECTAMENTE (sin binario git, determinista): branch, remote, remoteUrl, lastCommits (hasta 5). No es repo → { branch: null, remote: null } (no falla). Base de la feature de documentación (rama/GitHub/commits). Gherkin `features/git.feature`. 84 tests verdes (21 archivos).
- **2026-08-26: Wave 3 — detector de versiones**: `src/context/versions.ts` — `versionsInfo(dir)` lee package.json: prod (dependencies) + dev (devDependencies), ordenado alfabéticamente. Sin package.json → { prod: {}, dev: {} } (no falla). "Versiones de lo instalado separado y ordenado" (feature de Cris). Gherkin `features/versions.feature`. 86 tests verdes (22 archivos).
- **2026-08-26: Wave 3 — detector de cobertura**: `src/context/coverage.ts` — `coverageInfo(dir)` lee `coverage/coverage-summary.json` (vitest/istanbul json-summary): lines/functions/branches/statements (%). Sin coverage → 0 (no falla). "Qué % del proyecto está cubierto por tests" (feature de Cris). Gherkin `features/coverage.feature`. 88 tests verdes (23 archivos).
- **2026-08-26: Wave 3 — detector de servicios**: `src/context/services.ts` — `servicesInfo(dir)` detecta servicios conectados (frontend/backend/urls/puertos) desde package.json scripts (dev/start con --port). Sin servicios → { services: [] } (no falla). "Detectar los servicios de infraestructura" (feature de Cris). Gherkin `features/services.feature`. 90 tests verdes (24 archivos).
- **2026-08-26: Wave 3 — detectores dirs + todos**: `src/context/dirs.ts` (`dirsTree(dir)` árbol de carpetas hasta depth 3, excluye node_modules/.git) + `src/context/todos.ts` (`todosInfo(dir)` escanea TODO/FIXME en código). "Carpetas/subcarpetas" + "pendientes" (features de Cris). Gherkin `features/dirs-todos.feature`. **Wave 3 (detectores) COMPLETA: git + versions + coverage + services + dirs + todos.** 94 tests verdes (25 archivos).
- **2026-08-26: Wave 3 — snapshot store**: `src/context/snapshot.ts` — `buildSnapshot(dir)` une los 6 detectores (git/versions/coverage/services/dirs/todos) en un `ProjectSnapshot` con mtime; `saveSnapshot` persiste en `.netrunner/state/project.json`; `loadSnapshot` lee (o null). Es el "sticky note" vivo y progresivo del proyecto (feature de Cris). Gherkin `features/snapshot.feature`. 97 tests verdes (26 archivos).
- **2026-08-26: Wave 3 — generador de doc viva**: `src/generate/index.ts` — `generateDocs(dir)` genera `README.generated.md` (stack/git/versiones/cobertura/servicios/pendientes desde el snapshot) + `AGENTS.md` (instrucciones para agentes). Idempotente (sobreescribe). Es la feature de Cris: "forzar la creación de documentación del proyecto, con el agente y automáticamente". Gherkin `features/generate.feature`. 99 tests verdes (27 archivos).
- **2026-08-26: Wave 3 — CLI status (sticky note vivo)**: `netrunner status` devuelve el snapshot del proyecto (git/versions/coverage/services/dirs/todos) en JSON TOON; `netrunner status --docs` además regenera README.generated.md + AGENTS.md. Verificado en Bun real (rama develop, remoto GitHub, commits). Gherkin `features/status.feature`. **Wave 3 (skill auto-generante) COMPLETA: detectores + snapshot + generador + CLI status.** 101 tests verdes (28 archivos).
- **2026-08-26: Wave 4 (features cyberpunk) — persist**: `src/persist/index.ts` — `persistDecision(dir, decision, autor)` escribe `.netrunner/decisions/<slug>.md` con provenance (fecha/autor/estado open). Decisiones durables que sobreviven la sesión (el "virus que persiste tras Jack-Out" de la mina cyberpunk #7). Idempotente (mismo slug → sobreescribe). Gherkin `features/persist.feature`. 103 tests verdes (29 archivos).
- **2026-08-26: Wave 4 — map (grafo visual)**: `src/map/index.ts` — `exportMap(dir)` exporta el grafo (index.db) a JSON visualizable (D3/mermaid): { nodes, edges } con id/name/kind/file. La "matrix" de Case (mina cyberpunk #3). Sin index.db → { nodes: [], edges: [] } (no falla). Acotado a 200 nodos (TOON). Gherkin `features/map.feature`. 105 tests verdes (30 archivos).
- **2026-08-26: Wave 4 — scan (overlay de info)**: `src/scan/index.ts` — `scanProject(dir)` unifica stack + git + versions + coverage + services + todos en una llamada (la "Smartlink/Kiroshi" de la mina cyberpunk #23). Reutiliza los detectores (no duplica). Sin datos → defaults (no falla). Gherkin `features/scan.feature`. 107 tests verdes (31 archivos).
- **2026-08-26: Wave 4 — guard (Black ICE)**: `src/guard/index.ts` — `guardCheck(dir)` detecta secrets (ghp_/sk-/token=) y archivos protegidos (.env/.pem/.key) que no deberían commitearse. Protege el proyecto del agente (Mandamiento 7). Sin issues → { ok: true, issues: [] }. Gherkin `features/guard.feature`. 110 tests verdes (32 archivos).
- **2026-08-26: Wave 4 — depth (disclosure por niveles)**: `src/depth/index.ts` — `depthQuery(symbol, level, dir)` consulta por niveles (L0 básico, L2 +callers, L3 +impact) pagando tokens solo por la profundidad necesaria (los "floors" de la NET Architecture, mina cyberpunk #5). Reutiliza queries.ts (callers/impact esperan el ID completo del nodo). Nivel inválido → error; no encontrado → { found: false }. Gherkin `features/depth.feature`. 113 tests verdes (33 archivos).
- **2026-08-26: Wave 4 — snapshot rollback**: `src/rollback/index.ts` — `createSnapshot(dir)` guarda el estado en `.netrunner/backups/<ts>.json`; `listSnapshots(dir)` lista; `restoreSnapshot(dir, id)` re-escribe. Backup/restore antes de cambios riesgosos (mina cyberpunk #19). Sin backups → { snapshots: [] } (no falla). Gherkin `features/rollback.feature`. **Wave 4 (features cyberpunk) COMPLETA: persist + map + scan + guard + depth + rollback.** 116 tests verdes (34 archivos).
- Repo: github.com/Christopher-Sch-dev/netrunner (público, main).
- Stack: TypeScript + Bun + pnpm · Target T0 · dsh + AXI.
- Referencias: colbymchenry/codegraph, Graphify-Labs/graphify, deepseek-ai/deepseek-harness, kunchenguid/axi, agent-plugins.org, modelcontextprotocol.io, Vercel AI SDK v7.
- MVP 90 días: netrunner init / install / policy + fork de auto-revisión.

## Historia reciente
- 2026-08-20: inicialización del repo — .onyx (PROJECT/RULES/DECISIONS/TESTING/CONTEXT), spec.md raíz (destraba target-check), DEC-001 (decisión fundacional).
- 2026-08-20: re-validación 2026 (3 auditorías) → DEC-002: se mantiene TS+Bun+pnpm + motor-unificado + pipeline, con ajustes (MCP 2026-07-28 stateless, ACP v2, sandbox separado, context-graph+MemGPT, property-based+golden, Vitest). Reportes en `.doc/auditoria/` (gitignored).
- 2026-08-20: `src/context/detect.ts` — detección determinista de stack por manifestos (package.json+lockfiles→ts/js pnpm/npm/yarn/bun + framework astro/react/next/vite, pyproject/requirements→python pip/poetry/uv, Cargo→rust, go.mod→go; manifiesto ilegible→ausente). `detectStack(dir): Promise<StackInfo>` (consume types.ts). Tests `tests/detect.test.ts` (16 casos, AC-D1..D7), mutation 82.88%.
- 2026-08-20: `src/context/queries.ts` — queries de lectura del grafo SQLite (index.db, AC-5): `explore(name)`, `callers(symbolId)`, `callees(symbolId)`, `impact(symbolId, depth=2)` (BFS acotado). Vía bun:sqlite, LIMIT 100 nodos con `truncated=true`. Tests `tests/queries.test.ts` (6 casos, AC-5.1..5.5) con mock de bun:sqlite vía node:sqlite para correr bajo vitest; Gherkin `features/queries.feature`.
- 2026-08-20: `src/context/parse.ts` — parser de símbolos con tree-sitter WASM (typescript/tsx/javascript, python, go, rust). `parseFile(code, lang)` → defs/imports/calls (símbolos planos sin id/file). `parseDefinitions/parseImports/parseCalls`. Tests `tests/parse.test.ts` (5 casos, AC-G1..G4). Deps: web-tree-sitter + tree-sitter-wasms.
- 2026-08-20: `src/context/types.ts` — ancla de tipos (StackInfo/GraphNode/GraphEdge/QueryResult).
- 2026-08-20: `src/context/graph.ts` — indexador del grafo (AC-1/AC-5): `indexProject(dir, {incremental})` recorre archivos fuente → parseFile → persiste en `<proyecto>/.netrunner/index.db` (tablas nodes/edges, CREATE IF NOT EXISTS). incremental=true solo re-indexa archivos con mtime cambiado (tabla index_meta). Idempotente. Usa bun:sqlite (para correr en Bun runtime; vitest lo mockea). Tests `tests/graph.test.ts` (3 casos, AC-G1..G4). Completa la Wave 1 (detect + parse + graph + queries).
- 2026-08-20: `src/transport/mcp-server.ts` — servidor MCP (AC-3 vista MCP, AC-9 progressive disclosure). `createServer(dir)` construye el McpServer (SDK @modelcontextprotocol 1.30, zod 4); `serveMCP(dir)` arranca por stdio. **DIFERENCIADOR DE TOKENS**: al conectar expone SOLO meta-tools (net_available_toolsets, net_enable_toolset); al habilitar un toolset (derivado del stack) registra dinámicamente sus tools (net_explore/callers/callees/impact/net_stack). Idempotente (no duplica). Tests `tests/mcp-server.test.ts` (4 casos, AC-M1..M3) con InMemoryTransport+Client real (conformance MCP). Deps: @modelcontextprotocol/sdk, zod.
- 2026-08-20: `src/cli.ts` — CLI agent-friendly (AC-4 dashboard, AC-6 ops, AC-14 ai-native-cli). JSON output por defecto, `--human`, exit 0/1/2, errores estructurados a stderr. Comandos: (sin arg)=dashboard, `init <dir>` (indexa grafo), `plan "<goal>"`, `explore <sym>`, `--mcp` (arranca server MCP stdio), `--version`. Tests `tests/cli.test.ts` (3 casos, AC-4/14). **`graph.ts` usa bun:sqlite** (no node:sqlite) para que el CLI corra en Bun runtime (node:sqlite no existe en Bun) — vitest lo mockea.
- 2026-08-26: **Deuda M1 pagada** (auditoría externa): `parse.ts` dividido en `parse-specs.ts` + `parse-imports.ts` (parse 306→168). `graph.ts:indexProject` extrajo `applyParsed`. `queries.ts:impact` extrajo `expandBfs`. `mcp-server:registerToolsetTools` extrajo `registerGraphTools`+`registerStackTool`. Todos los módulos <200 líneas.
- 2026-08-26: **Desvío AC-3/DEC-005 §3 pagado** (auditoría externa): `mcp-server.ts` YA NO reimplementa handlers. Ahora **proyecta el ToolRegistry central** (`buildNetrunnerRegistry` en `src/core/registry-factory.ts`): las tools viven como ToolSpec en `src/tools/index.ts` (graph.explore/callers/callees/impact, stack.info) con execute por DI (ToolContext), y la vista MCP las registra delegando en `registry.call()`. Cualquier tool nueva del contrato aparece sola en MCP. 42 tests verdes.
- 2026-08-26: **Desvío CLI AXI pagado**: `netrunner plan "<goal>"` ya NO es placeholder — genera un **plan REAL accionable** (`generatePlan`) con `steps[]` derivados del grafo indexado (símbolos/archivos), y **output TOON** por defecto (sin campo `context` verboso; solo `{goal, steps}`). `plan` sin goal → error estructurado `MISSING_REQUIRED` exit 2. Gherkin `features/cli-plan.feature`. 45 tests verdes (8 archivos).
- 2026-08-26: **w2a3 completo — conector ripgrep**: `src/tools/rg.ts` (`rgSearch(pattern, dir)` ejecuta `rg --json`, respeta `.gitignore`/excluye node_modules por `--glob`, acotado a LIMIT 200 matches TOON). Es **ToolSpec del contrato** (`search.rg`, family graph, readOnly) registrada en `graphAndStackTools()` → se proyecta sola en MCP (`net_rg`). Gherkin `features/rg.feature`. **Wave 2 COMPLETA** (MCP + CLI + rg). 48 tests verdes (9 archivos).
- 2026-08-26: **DEC-006 + w3a2/w3a3 — formato de instalación y `netrunner install`**: dictamen = **Agent Skills (`SKILL.md`) + wiring MCP** (no Agent Plugin monolítico ni SLI custom, DEC-006). `src/install.ts` (`install(target, dir)`) escribe `.netrunner/skills/netrunner/SKILL.md` (frontmatter name/description + cómo operar) + registra `netrunner --mcp` en config del target (mcp/opencode/claude/cursor), idempotente. Comando CLI `netrunner install mcp`. Verificado en Bun real. Gherkin `features/install.feature`. 51 tests verdes (10 archivos).
- 2026-08-26: **Wave 4 COMPLETA — matrix de harnesses**: (w4a1) `src/harness/acp.ts` — adapter ACP (SDK @agentclientprotocol 1.4.0), expone el motor como agente ACP por stdio proyectando el ToolRegistry (DEC-005 §3); (w4a2) `install --target` ampliado a matrix (mcp/opencode/claude/cursor/codex/gemini/hermes/dsh/fx); (w4a3) `src/plugin/generate.ts` — Agent Plugin 1.0.0 generator (plugin.json + skills/ + mcp.json). Comandos CLI `netrunner install <target>` y `netrunner plugin <name> <version>`. 57 tests verdes (12 archivos).
- 2026-08-26: **Wave 5 COMPLETA — auto-mejora + policy + hardening**: (w5a1) `src/auto/curator.ts` — curator determinista PURE (auto-mejora con señal EXTERNA, nunca auto-crítica, Mandamiento 8): uso exitoso → upsert_skill (Memento), stale → mark_review (nunca borra), vacío → no-op; (w5a2) `src/policy/index.ts` — policy cross-client PURE (parity: misma intención+contexto = misma decisión en cualquier agente, fail-closed); (w5a3) `tests/cross-os.test.ts` — paths portables via node:path. **MOTOR COMPLETO: Waves 1-5 hechas.** 67 tests verdes (15 archivos).
